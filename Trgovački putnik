#include <stdio.h>
#include <stdint.h>
 
 
void sort(int p1[], int p2[], int n){
    int min;
    int tmp;
   
    for(int i = 0; i < n; i++){
        min = i;
        for(int j = i + 1; j < n; j++){
            if(p1[j] > p1[min]){
                min = j;
            }
        }
        tmp = p1[i];
        p1[i] = p1[min];
        p1[min] = tmp;
 
        tmp = p2[i];
        p2[i] = p2[min];
        p2[min] = tmp;
 
    }
}
 
 
int main(){
    int x, y, quan[1000] = {0}, pop[1000] = {0}, tmp;
 
    scanf("%d", &x);
    for(int i = 0; i < x; i++){
        scanf("%d", &tmp);
        quan[tmp - 1]++;
    }
    for(int i = 0; i < x; i++){
        pop[i] = i + 1;
    }
    scanf("%d", &y);
 
    sort(quan, pop, x);
 
    for(int i = 0; i < y; i++){
        printf("%d\n", pop[i]);
    }
}
